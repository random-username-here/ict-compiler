# IVM-specific things

Here are:

 - built ivm & iasm (TODO: move them out of git)
 - scl standard library in `inc/`
 - assembly routines in `asm/`
 - "linker", made out of one `link.s`, which includes everything
 - `run.sh` script for easy compiling & running any scl program
